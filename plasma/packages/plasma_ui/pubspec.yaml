name: plasma_ui
description: Shared UI components for Plasma SDK - theme, buttons, and common widgets.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  plasma_core:
    path: ../plasma_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
